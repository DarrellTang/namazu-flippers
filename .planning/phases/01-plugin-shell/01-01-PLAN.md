---
phase: 01-plugin-shell
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - NamazuFlippers/NamazuFlippers.csproj
  - NamazuFlippers/NamazuFlippers.sln
  - NamazuFlippers/NamazuFlippers.json
  - NamazuFlippers/NamazuFlippers.cs
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - PLUG-03

must_haves:
  truths:
    - "`.dotnet build` compiles without errors"
    - "Plugin manifest is valid JSON matching Dalamud spec"
    - "Plugin entry point implements IDalamudPlugin interface"
    - "Plugin namespace is `NamazuFlippers`"
  artifacts:
    - path: "NamazuFlippers/NamazuFlippers.sln"
      provides: "Solution file for the plugin project"
    - path: "NamazuFlippers/NamazuFlippers.csproj"
      provides: "Project file targeting .NET 8 with Dalamud SDK dependency"
    - path: "NamazuFlippers/NamazuFlippers.json"
      provides: "Dalamud plugin manifest with /nflip command"
    - path: "NamazuFlippers/NamazuFlippers.cs"
      provides: "Plugin entry point class implementing IDalamudPlugin"
  key_links:
    - from: "NamazuFlippers.csproj"
      to: "DalamudPackager"
      via: "Project SDK reference"
      pattern: "DalamudPackager"
    - from: "NamazuFlippers.json"
      to: "NamazuFlippers.dll"
      via: "AssemblyName match"
      pattern: "NamazuFlippers"
    - from: "NamazuFlippers.cs"
      to: "IDalamudPlugin"
      via: "Interface implementation"
      pattern: "IDalamudPlugin"
---

<objective>
Create a minimal compileable Dalamud plugin scaffold: solution, project file, plugin manifest, and entry point class. This is the skeleton everything else attaches to.
**Purpose**: Establish the .NET/Dalamud build foundation so subsequent plans can add features.
**Output**: `NamazuFlippers.sln`, `NamazuFlippers.csproj`, `NamazuFlippers.json`, `NamazuFlippers.cs` — compiles via `dotnet build`.
</objective>

<execution_context>
@~/.pi/gsd/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md §Phase 1
@.planning/REQUIREMENTS.md §PLUG-01, PLUG-02, PLUG-03
@.planning/phases/01-plugin-shell/01-CONTEXT.md
@SPEC.md §Plugin Architecture
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DALAMUD project scaffold</name>
  <files>
    NamazuFlippers/NamazuFlippers.sln
    NamazuFlippers/NamazuFlippers.csproj
  </files>
  <action>
Create a standard Dalamud plugin project targeting .NET 8+:

1. Create solution file:
   ```bash
   dotnet new sln -n NamazuFlippers -o NamazuFlippers
   ```

2. Create project file for a Dalamud plugin library. The `.csproj` must:
   - Target `net8.0-windows` (Dalamud plugins require Windows TFM)
   - Set `<AllowUnsafeBlocks>true</AllowUnsafeBlocks>` (required for Dalamud interop)
   - Set `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>` (Dalamud convention)
   - Set `<DalamudLibPath>$(appdata)\XIVLauncher\addon\Hooks\dev</DalamudLibPath>` (standard Dalamud dev path, use `$env:APPDATA` on Windows, fallback to configurable path)
   - Reference `DalamudPackager` as a build target (inline in .csproj, per Dalamud docs)
   - Include `<PackageReference Include="DalamudLinter" Version="1.0.0" PrivateAssets="all" />` for linting
   - Set `<AssemblyName>NamazuFlippers</AssemblyName>`
   - Set `<RootNamespace>NamazuFlippers</RootNamespace>`

3. Add the project to the solution:
   ```bash
   dotnet sln NamazuFlippers/NamazuFlippers.sln add NamazuFlippers/NamazuFlippers.csproj
   ```

Per D-02: namespace and assembly name is `NamazuFlippers`.
Per D-07: minimal scaffold — only these files, no subdirectories yet.
  </action>
  <verify>
    <automated>dotnet restore NamazuFlippers/NamazuFlippers.csproj && echo "Restore OK"</automated>
  </verify>
  <done>.sln and .csproj exist, `dotnet restore` succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create plugin manifest and entry point</name>
  <files>
    NamazuFlippers/NamazuFlippers.json
    NamazuFlippers/NamazuFlippers.cs
  </files>
  <action>
Create the two core plugin files:

**NamazuFlippers/NamazuFlippers.json** — Dalamud plugin manifest:
- `Name`: "Namazu Flippers" (per D-01)
- `Author`: "[your handle]"
- `Punchline`: "Daily cross-server arbitrage route — flip items, make gil."
- `Description`: "Namazu Flippers connects to the Saddlebag Exchange API to find daily cross-server arbitrage opportunities. Get a route-optimized shopping list, travel between servers, buy low, sell high on your home world. One scan, one route, done in under 20 minutes."
- `InternalName`: "NamazuFlippers" (per D-02)
- `AssemblyVersion`: "1.0.0.0"
- `RepoUrl`: "https://github.com/darrell-tang-consulting/namazu-flippers"
- `ApplicableVersion`: "any"
- `DalamudApiLevel`: Use the latest stable Dalamud API level (check XIVLauncher/Dalamud repo for current)
- `Tags`: `["arbitrage", "market", "gil", "trading", "cross-server"]`
- `IconUrl`: "" (leave empty for now)
- `LoadRequiredState`: 0
- `LoadSync`: false
- `LoadPriority`: 0
- `CanUnloadAsync`: false

**NamazuFlippers/NamazuFlippers.cs** — Plugin entry point:
- Namespace: `NamazuFlippers`
- Class: `NamazuFlippers` implementing `IDalamudPlugin`
- Fields: `DalamudPluginInterface PluginInterface`, `CommandManager CommandManager`, `Configuration Configuration` (full config type added in Plan 01-02; use a placeholder `Configuration` stub class for now with just `HomeWorld` property)
- Import required Dalamud namespaces: `Dalamud.Game.Command`, `Dalamud.Plugin`, `Dalamud.IoC`
- Constructor: accepts `DalamudPluginInterface`, stores it, registers `/nflip` command (per D-03) via `CommandManager.AddHandler("/nflip", ...)`
- Command handler: toggles visibility. For now, log a placeholder message to PluginLog since no UI window exists yet (that comes in Phase 4)
- `Dispose()`: unregisters `/nflip` command via `CommandManager.RemoveHandler("/nflip")`, disposes any resources. Must not throw (per SC-5: clean disposal)
- Apply `[DalamudPlugin]` attribute or follow the standard Dalamud entry-point convention for the current API level

Per PLUG-01: manifest is valid for XIV Launcher loading.
Per PLUG-02: `/nflip` command is registered.
Per PLUG-03: constructor/dispose follow Dalamud lifecycle.
Per D-03: chat command is `/nflip`.
  </action>
  <verify>
    <automated>dotnet build NamazuFlippers/NamazuFlippers.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Plugin compiles, manifest.json is valid JSON, entry point registers /nflip command, Dispose cleans up</done>
</task>

<task type="auto">
  <name>Task 3: Add first-run home world ImGui popup</name>
  <files>NamazuFlippers/NamazuFlippers.cs</files>
  <action>
Add a simple ImGui popup window for first-run home world selection to `NamazuFlippers.cs`:

1. Add a boolean flag `isFirstRun` initialized to `true` and a `string` field `pendingHomeWorld` for the popup input.

2. In the command handler (or a plugin UI update method), when `/nflip` is toggled AND the home world is not yet set AND `isFirstRun` is true:
   - Show a small ImGui window (begin/end + popup modal) with:
     - Title: "Welcome to Namazu Flippers"
     - Text: "Enter your home world to get started"
     - An `ImGui.InputText` field for the user to type their home world name
     - A "Confirm" button that saves the value to `Configuration.HomeWorld` and dismisses the popup
   - After confirm: `isFirstRun = false`, call `DalamudPluginInterface.SavePluginConfig(Configuration)` to persist

3. The popup should auto-appear the first time `/nflip` is used. It should NOT show again after a home world is set and saved.

4. If home world is already set in config on load (from persisted config), skip the popup entirely.

Per D-09: simple ImGui popup with text input + confirm button.
Per D-10: auto-appears on first `/nflip`, dismisses once home world is saved.
Per SC-3: home world prompt appears on first run and persists.

Note: This uses a stub `Configuration` class (just `HomeWorld` property) that will be replaced by the full class in Plan 01-02. The two plans should coordinate — 01-02's Configuration class replaces this stub.
  </action>
  <verify>
Build must succeed. Verify the popup logic: check that `isFirstRun` guard, `Configuration.HomeWorld` check, ImGui window code, and save-on-confirm path all compile and the control flow is correct.
    <automated>dotnet build NamazuFlippers/NamazuFlippers.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Plugin compiles with first-run popup logic intact; control flow handles unset home world, set home world, and save-on-confirm</done>
</task>

</tasks>

<verification>
- [ ] `dotnet restore` succeeds
- [ ] `dotnet build` compiles with zero errors
- [ ] Solution opens correctly: `NamazuFlippers/NamazuFlippers.sln`
- [ ] Manifest.json passes JSON validation (check against Dalamud manifest schema)
</verification>

<success_criteria>
1. `dotnet build NamazuFlippers/NamazuFlippers.csproj` compiles cleanly with zero errors and zero warnings (or only non-blocking warnings)
2. Solution file exists and references the project correctly
3. Plugin manifest is valid JSON with correct InternalName matching AssemblyName
4. Entry point implements IDalamudPlugin with constructor accepting DalamudPluginInterface
5. `/nflip` command is registered in constructor and unregistered in Dispose
6. First-run home world popup control flow is in place (ImGui window, confirm button, save)
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-shell/01-01-SUMMARY.md`
</output>

---

## Architecture Review Notes (post-execution)

Applied from ARCHITECTURE_REVIEW_NOTES.md after Phase 1 execution:

- ✅ **Separate UI from lifecycle** — First-run popup extracted to `FirstRunWindow.cs`. Entry point stays thin.
- ✅ **Named category constants** — `FurnitureIds`, `CollectibleIds`, `GlamourIds`, `DefaultCategoryFilters` in Configuration.cs
- ✅ **Mutable array caution** — `CategoryFilters` and `PreferredCategories` documented as mutable; copy before exposing
- ✅ **`.gitignore`** — Added for bin/, obj/, .firecrawl/, .pi/
- 🔲 **Validation boundaries** — Deferred to Phase 4 (ConfigWindow) and Phase 3 (ScanEngine)
- 🔲 **Service locator risk** — Will be addressed as SaddlebagClient, ScanEngine, RouteOptimizer are built in Phases 2-3
- 🔲 **C# practices** (CancellationToken, HttpClient lifetime, async) — Apply in Phase 2 (API client)

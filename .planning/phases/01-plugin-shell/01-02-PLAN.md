---
phase: 01-plugin-shell
plan: 01-02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - NamazuFlippers/Configuration.cs
  - NamazuFlippers/NamazuFlippers.cs
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - CONF-06
  - CONF-07
  - CONF-08
  - CONF-09

must_haves:
  truths:
    - "All 9 config settings persist across game session restarts"
    - "Configuration class has typed properties for every CONF-01 through CONF-09"
    - "Config loads automatically on plugin startup"
    - "Config saves automatically when settings change"
    - "Default values match SPEC.md ConfigWindow Settings table"
  artifacts:
    - path: "NamazuFlippers/Configuration.cs"
      provides: "Typed configuration class with all 9 settings and defaults"
      contains: "class Configuration"
    - path: "NamazuFlippers/NamazuFlippers.cs"
      provides: "Plugin entry updated to use full Configuration class with load/save"
  key_links:
    - from: "NamazuFlippers.cs"
      to: "Configuration.cs"
      via: "PluginInterface.GetPluginConfig|SavePluginConfig"
      pattern: "GetPluginConfig|SavePluginConfig"
---

<objective>
Replace the placeholder Configuration stub with a full configuration class covering all 9 settings (CONF-01 through CONF-09), integrated with Dalamud's built-in config serialization for persistence across game sessions.
**Purpose**: Establishes the data model for all plugin settings. Subsequent phases (API client, scan engine, UI) read from this config. Not building the ConfigWindow UI yet — that's Phase 4.
**Output**: `NamazuFlippers/Configuration.cs` with all 9 properties + defaults, wired into plugin entry point for load-on-startup and save-on-change.
</objective>

<execution_context>
@~/.pi/gsd/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md §Key Decisions
@.planning/ROADMAP.md §Phase 1
@.planning/REQUIREMENTS.md §CONF-01 through CONF-09
@.planning/phases/01-plugin-shell/01-CONTEXT.md §Configuration storage
@SPEC.md §ConfigWindow Settings (default values table)
@SPEC.md §Full API Endpoint Reference (/api/scan parameters)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Configuration class with all settings</name>
  <files>NamazuFlippers/Configuration.cs</files>
  <action>
Create `NamazuFlippers/Configuration.cs` as a standalone file (per D-07: minimal scaffold). The class must:

**Base structure:**
- Namespace: `NamazuFlippers`
- Class: `Configuration` — implements `IPluginConfig` (Dalamud interface for built-in serialization, per D-05)
- All properties use `{ get; set; }` (Dalamud's JSON serializer needs both accessors)

**Properties (with defaults from SPEC.md):**

| Property | Type | Default | Requirement |
|----------|------|---------|-------------|
| `HomeWorld` | `string` | `""` (empty) | CONF-01 — triggers first-run popup when empty |
| `PreferredRoi` | `int` | `25` | CONF-02 — minimum ROI% (25 = 25%) |
| `MinProfitAmount` | `int` | `10000` | CONF-02 — minimum gil profit per item |
| `MinDesiredAvgPpu` | `int` | `10000` | CONF-02 — minimum avg price on home server |
| `MinSalesPerWeek` | `int` | `2` | CONF-03 — velocity floor for scan |
| `RegionWide` | `bool` | `false` | CONF-04 — search all DCs (true) or local only (false) |
| `CategoryFilters` | `int[]` | `[56,65,66,67,68,69,70,71,72,81,82, 75,80,90, 1,2,3,4,-5]` | CONF-05 — category/subcategory IDs from SPEC.md Proven Item Categories table. Default: Furniture + Collectibles + Glamour |
| `IncludeVendors` | `bool` | `true` | CONF-06 — vendor items as purchase sources |
| `ShowOutOfStock` | `bool` | `true` | CONF-06 — include items with zero home listings |
| `MaxItemsPerSession` | `int` | `10` | CONF-07 — cap on route size |
| `MaxServersToVisit` | `int` | `10` | CONF-07 — server hop budget |
| `CacheDurationHours` | `int` | `4` | CONF-08 — scan cache validity window |
| `EnableShortagePredictor` | `bool` | `false` | Optional supplement (Phase 6) — included now for config completeness |
| `PreferredCategories` | `string[]` | `["Furniture", "Collectibles", "Glamour"]` | Human-readable category labels matching toggle states — used by Phase 4 ConfigWindow to render category toggles |

**Additional context for the Configuration class:**
- The class should be a plain C# POCO — no logic, just data. Validation logic belongs elsewhere (Phase 4 ConfigWindow or Phase 3 ScanEngine).
- `IPluginConfig` interface: implement `int Version { get; set; }` for future migration support (start at 1).
- Per D-05: Dalamud built-in serialization handles JSON round-tripping automatically via `DalamudPluginInterface.GetPluginConfig<T>()` and `SavePluginConfig<T>()`.

Per all CONF-01 through CONF-09 requirements.
Per D-06: config model includes all CONF-01 through CONF-09 with sensible defaults.
  </action>
  <verify>
    <automated>dotnet build NamazuFlippers/NamazuFlippers.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Configuration.cs compiles, has properties for all 9 CONF requirements, implements IPluginConfig, default values match SPEC.md</done>
</task>

<task type="auto">
  <name>Task 2: Wire config into plugin lifecycle</name>
  <files>NamazuFlippers/NamazuFlippers.cs</files>
  <action>
Update `NamazuFlippers.cs` to replace the Configuration stub with the full Configuration class:

1. **Remove the placeholder Configuration stub** — delete the inline stub class from NamazuFlippers.cs.

2. **Load config on startup** — in the constructor, after storing `PluginInterface`:
   ```csharp
   Configuration = PluginInterface.GetPluginConfig<Configuration>() ?? new Configuration();
   ```
   This loads existing config from Dalamud's config directory, or initializes with defaults on first run. Per CONF-09: all settings persist across sessions.

3. **Save config on first-run popup confirm** — the existing first-run popup (from Plan 01-01 Task 3) already calls `SavePluginConfig(Configuration)` after setting HomeWorld. Ensure this still compiles with the new full Configuration class.

4. **Update first-run check** — the `isFirstRun` guard should check `string.IsNullOrEmpty(Configuration.HomeWorld)` using the property from the full Configuration class.

5. **Add config ownership comment** — add a comment noting that config save-on-change for the full ConfigWindow (Phase 4) will use the same `SavePluginConfig<T>()` call. For now, only the first-run popup triggers saves.

Per D-05: uses Dalamud built-in config serialization.
Per CONF-09: config persists across sessions (handled automatically by Dalamud's `GetPluginConfig`/`SavePluginConfig`).
  </action>
  <verify>
    <automated>dotnet build NamazuFlippers/NamazuFlippers.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Plugin loads Configuration via GetPluginConfig on startup, first-run popup references full Configuration class, placeholder stub removed</done>
</task>

</tasks>

<verification>
- [ ] Configuration.cs implements IPluginConfig with Version field
- [ ] All 9 CONF property defaults match SPEC.md ConfigWindow Settings table
- [ ] NamazuFlippers.cs loads config via GetPluginConfig<Configuration>() in constructor
- [ ] First-run popup saves via SavePluginConfig(Configuration) after home world confirm
- [ ] No placeholder Configuration stub remains in NamazuFlippers.cs
- [ ] `dotnet build` compiles clean
</verification>

<success_criteria>
1. Configuration.cs compiles with typed properties for all CONF-01 through CONF-09
2. Plugin loads persisted config on startup (`GetPluginConfig<Configuration>()`)
3. Plugin saves config on first-run home world confirm (`SavePluginConfig(Configuration)`)
4. All defaults match SPEC.md ConfigWindow Settings table
5. No generic/placeholder config code remains — the full model is in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-shell/01-02-SUMMARY.md`
</output>
